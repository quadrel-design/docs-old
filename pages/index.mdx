// pages/index.tsx oder pages/index.mdx (wenn du MDX verwendest)
export async function getStaticProps() {
  return {
    props: {
      message: 'Hello, World!'
    }
  }
}

const HomePage = ({ message }: { message: string }) => {
  return <h1>{message}</h1>
}

export default HomePage;
